sudo bash -c 'set -e; export DEBIAN_FRONTEND=noninteractive; apt-get update -qq; apt-get install -y -qq ca-certificates curl unzip; reality_tmp=$(mktemp -d); trap "rm -rf -- \"$reality_tmp\"" EXIT; curl -fL --retry 3 --connect-timeout 15 --max-time 120 "https://raw.githubusercontent.com/manwithoutpant/rerere/main/reality-github-v1.1.zip" -o "$reality_tmp/package.zip"; unzip -p "$reality_tmp/package.zip" "reality-github/reality-oneclick.sh" > "$reality_tmp/reality-oneclick.sh"; bash "$reality_tmp/reality-oneclick.sh" install'
